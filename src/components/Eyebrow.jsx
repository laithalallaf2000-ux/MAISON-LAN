/** Small capitalised section label, preceded by an acid-green dot. */
export function Eyebrow({ children }) {
  return (
    <p className="section-label">
      <span />
      {children}
    </p>
  )
}
